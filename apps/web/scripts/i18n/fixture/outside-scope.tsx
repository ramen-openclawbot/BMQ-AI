// Deliberately excluded business pages/PO import: never counted as runtime coverage.
export default function OutsideScope() { return <p data-fixture-outside-scope>Outside Batch A fixture</p>; }
export const DriveImportProgressDialog = () => null;
