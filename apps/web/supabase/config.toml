project_id = "cxntbdvfsikwmitapony"

# ──────────────────────────────────────────────────────────
# Functions with JWT verification ENABLED (have auth checks in code)
# ──────────────────────────────────────────────────────────

[functions.scan-invoice]
# verify_jwt disabled: proxy uses HS256 but project issues ES256 user tokens.
# Auth handled inside function via requireAuth().
verify_jwt = false

[functions.scan-purchase-order]
# verify_jwt disabled: proxy uses HS256 but project issues ES256 user tokens.
# Auth handled inside function via getUser(token).
verify_jwt = false

# verify_jwt disabled: proxy/gateway JWT verification can reject valid ES256 user tokens
# before the function runs. scan-drive-folder now relies on frontend apikey + Bearer
# contract and performs only lightweight header checks in code.
[functions.scan-drive-folder]
verify_jwt = false

[functions.sync-drive-index]
# verify_jwt disabled: proxy uses HS256 but project issues ES256 user tokens.
verify_jwt = false

[functions.create-invoice-from-pr]
# verify_jwt disabled: proxy uses HS256 but project issues ES256 user tokens.
verify_jwt = false

[functions.match-delivery-note]
# verify_jwt disabled: proxy uses HS256 but project issues ES256 user tokens.
verify_jwt = false

[functions.create-warehouse-receipt]
# verify_jwt disabled: proxy uses HS256 but project issues ES256 user tokens.
verify_jwt = false

[functions.scan-bank-slip]
# verify_jwt disabled: proxy uses HS256 but project issues ES256 user tokens.
# Auth handled inside function via getUser(token).
verify_jwt = false

[functions.scan-sku-cost-sheet]
# verify_jwt disabled: proxy uses HS256 but project issues ES256 user tokens.
# Auth handled inside function via requireAuth().
verify_jwt = false

[functions.user-invite-member]
verify_jwt = false

[functions.user-delete-member]
verify_jwt = false

[functions.finance-extract-slip-amount]
# verify_jwt disabled: proxy uses HS256 but project issues ES256 user tokens.
# Auth handled inside function via requireAuth() → supabaseAdmin.auth.getUser(token).
verify_jwt = false

[functions.po-gmail-sync]
verify_jwt = false

[functions.get-ai-credit-balance]
verify_jwt = false

[functions.migration-storage-summary]
verify_jwt = false

[functions.kb-suggest-po-rules]
# verify_jwt disabled: proxy uses HS256 but project now issues ES256 user tokens
# (alg: ES256, kid present). Auth is handled inside the function via requireAuth()
# which calls supabaseAdmin.auth.getUser(token) — this correctly validates ES256.
verify_jwt = false

[functions.migration-storage-manifest]
verify_jwt = false

# ──────────────────────────────────────────────────────────
# Functions with JWT verification DISABLED
# Reason: OAuth callbacks (no JWT available) or Cron jobs (use x-cron-secret)
# ──────────────────────────────────────────────────────────

[functions.google-drive-auth]
verify_jwt = false

[functions.google-gmail-auth]
verify_jwt = false

[functions.finance-auto-reconcile]
verify_jwt = false

[functions.po-gmail-ingest]
verify_jwt = false

# ──────────────────────────────────────────────────────────
# Functions needing further audit before enabling JWT
# Keep verify_jwt = false until auth is added to the code
# ──────────────────────────────────────────────────────────

[functions.test-drive-connection]
verify_jwt = false

[functions.po-gmail-debug-check]
verify_jwt = false

[functions.po-parse-inbox-order]
verify_jwt = false

[functions.po-promote-latest-kingfood]
verify_jwt = false

[functions.po-use-latest-kingfood-today]
verify_jwt = false

[functions.po-parse-latest-kingfood-now]
verify_jwt = false

[functions.po-debug-kingfood-xlsx]
verify_jwt = false

[functions.migration-storage-archive]
verify_jwt = false

[functions.po-sync-scheduler-run]
# verify_jwt disabled: proxy uses HS256 but project issues ES256 user tokens.
# Auth is enforced inside the function via requireAuth().
verify_jwt = false

